{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Family",
  "type": "object",
  "required": ["family_size"],
  "properties": {
    "family_name": {
      "type": "string",
      "maxLength": 100,
      "description": "Apellido o nombre de la familia"
    },
    "family_size": {
      "type": "integer",
      "minimum": 1,
      "maximum": 50,
      "description": "Tamaño de la familia"
    },
    "head_of_family_id": {
      "type": ["integer", "null"],
      "description": "ID del cabeza de familia (refugee_id)"
    },
    "notes": {
      "type": "string",
      "maxLength": 500,
      "description": "Notas adicionales sobre la familia"
    }
  },
  "additionalProperties": false
}
